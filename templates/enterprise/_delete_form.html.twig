<form method="post" action="{{ path('app_enterprise_delete', {'id': enterprise.id}) }}" onsubmit="return confirm('Étes-vous sur de vouloir supprimer cette entreprise');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ enterprise.id) }}">
    <button class="btn btn-danger"><i class="bi bi-trash me-2"></i>Supprimer</button>
</form>
